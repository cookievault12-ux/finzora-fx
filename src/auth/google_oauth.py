"""Deprecated: this module explored a Google OAuth login approach, but the
owner chose simpler HTTP Basic Auth instead (no Google Cloud Console setup
required) — see src/auth/basic_auth.py, which is what's actually wired into
src/api/health.py and src/api/dashboard.py. Kept only as a reference in
case Google-account login is revisited later; not imported anywhere.
"""
